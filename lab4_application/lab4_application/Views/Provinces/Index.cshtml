@model IEnumerable<lab4_application.Models.Province>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProviceName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProviceName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProviniceCode }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProviniceCode }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProviniceCode })
        </td>
    </tr>
}

</table>
